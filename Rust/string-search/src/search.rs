
#[derive(Debug)]
#[derive(PartialEq)]
pub struct PatternOccurance {
    pub start: usize,
    pub end: usize
}

pub fn find_occurances(str: &str, pattern: &str) -> Vec<PatternOccurance> {
    //TODO: Implement
    let occurrences = Vec::new();
    return occurrences
}