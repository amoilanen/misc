app {
  database {
    url = "jdbc:postgresql://localhost:5432/invoice_generator"
    username = "postgres"
    password = "password"
    driver = "org.postgresql.Driver"
    maxConnections = 10
  }
  
  kafka {
    bootstrapServers = "localhost:9092"
    topic = "invoice-events"
    groupId = "invoice-generator-service"
    autoOffsetReset = "earliest"
  }
  
  gcp {
    projectId = "fake-project-id"
    bucketName = "invoice-pdfs"
    credentialsPath = null
    # For local development with fake-gcs-server
    endpoint = "http://localhost:4443"
    # For production, set to null to use default GCP endpoints
    # endpoint = null
  }
  
  server {
    host = "0.0.0.0"
    port = 8080
  }
} 